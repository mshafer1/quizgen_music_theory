sudo: false
language: ruby
node_js:
- '8'
branches:
  only:
  - master
  - gh-pages
cache:
  directories:
  - node_modules
before_install:
- npm update
install:
- npm install dev
- npm install jekyll
script:
- jekyll build -d _site
- npm run coverage
after_success:
- npm run coveralls
