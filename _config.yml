destination: _site
exclude: ['node_modules', 'jest.config.js', 'package.json', 'package-lock.json', 'coverage']

plugins:
  - jekyll-tidy

collections:
  quizes:
    output: true

defaults:
  - scope:
      path: ""
      type: "quizes"
    values:
      layout: quiz

key_width: 32

white_height: 190

black_height: 125

default_keyboard: 4

low_keyboard: 2

high_keyboard: 6
